recieve data over mqtt broker{}
get data from mqtt msg{}

recieve msg{
    write incoming msg in logging.log

    if input > max{
        input = max
    }
    if input < min {
        input = min
    }
    sum = sum + input
    amount = amount + 1

}

write in values.txt{
    "date - time: " datetime
    "max: " max
    "min: " min
    "gem: " sum/amount
}